INSERT INTO UserType (Type)
Values ('admin'), ('costumer');

SELECT * FROM UserType;

INSERT INTO BuildingType(Type)
Values ('Hotel'), ('Apartment'), ('Hostel');

SELECT * FROM BuildingType;

SELECT * FROM Rent;